% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling.R
\name{correlation_table}
\alias{correlation_table}
\title{Correlation Table with Confidence Intervals}
\usage{
correlation_table(
  data,
  vars,
  method = c("pearson", "spearman", "kendall"),
  adjust = c("none", "bonferroni", "holm", "fdr"),
  ci_level = 0.95
)
}
\arguments{
\item{data}{Data frame}

\item{vars}{Character vector of variable names to correlate}

\item{method}{Character. Correlation method: "pearson" (default), "spearman", "kendall"}

\item{adjust}{Character. Multiple comparison adjustment: "none" (default), "bonferroni", "holm", "fdr"}

\item{ci_level}{Numeric. Confidence level (default 0.95)}
}
\value{
A tibble with correlations including:
\itemize{
  \item Variable pairs
  \item Correlation coefficient
  \item Confidence interval
  \item P-value (adjusted if requested)
  \item Sample size
}
}
\description{
Calculate correlations between multiple variables with confidence intervals.
Uses correlation package for comprehensive output.
}
\examples{
\dontrun{
df <- data.frame(
  satisfaction = rnorm(100),
  loyalty = rnorm(100),
  value = rnorm(100)
)

cors <- correlation_table(df, c("satisfaction", "loyalty", "value"))
print(cors)
}

}
