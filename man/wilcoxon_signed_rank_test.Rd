% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric.R
\name{wilcoxon_signed_rank_test}
\alias{wilcoxon_signed_rank_test}
\title{Wilcoxon Signed-Rank Test}
\usage{
wilcoxon_signed_rank_test(
  data = NULL,
  before = NULL,
  after = NULL,
  x = NULL,
  y = NULL,
  alternative = c("two.sided", "less", "greater")
)
}
\arguments{
\item{data}{A data frame containing the variables (optional if x and y are vectors)}

\item{before}{Character; name of the "before" variable (if data provided)}

\item{after}{Character; name of the "after" variable (if data provided)}

\item{x}{Numeric vector for time 1 (if data not provided)}

\item{y}{Numeric vector for time 2 (if data not provided)}

\item{alternative}{Direction of test: "two.sided" (default), "greater", or "less"}
}
\value{
A list of class "wilcoxon_signed_rank" containing:
  - statistic: V statistic
  - p_value: p-value
  - rank_biserial: Effect size
  - n_pairs: Number of pairs
  - median_diff: Median of differences
  - interpretation: Publication-ready text
}
\description{
Performs the Wilcoxon signed-rank test for paired samples when normality
assumptions are violated. This is the non-parametric alternative to the
paired t-test, used for pre-post designs or matched pairs.
}
\examples{
set.seed(42)
df <- data.frame(
  pre = rpois(50, 4),
  post = rpois(50, 6)
)

result <- wilcoxon_signed_rank_test(df, before = "pre", after = "post")
print(result)
}
