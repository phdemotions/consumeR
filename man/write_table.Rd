% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_formatting.R
\name{write_table}
\alias{write_table}
\title{Write Table to File}
\usage{
write_table(df, path, format = c("tsv", "csv", "md", "rds"), digits = 2, ...)
}
\arguments{
\item{df}{Data frame to save}

\item{path}{Character. File path for output}

\item{format}{Character. Output format: "tsv" (default), "csv", "md", or "rds"}

\item{digits}{Integer. Number of digits for rounding numeric columns (default 2)}

\item{...}{Additional arguments passed to format-specific functions}
}
\value{
Invisible path to saved file
}
\description{
Saves a data frame to disk in various formats (TSV, CSV, markdown, RDS).
}
\details{
**Format options:**
- "tsv": Tab-separated values (readable by Excel, R, Python)
- "csv": Comma-separated values
- "md": Markdown table (for documentation)
- "rds": R data serialization (preserves exact data types)
}
\examples{
\dontrun{
df <- data.frame(
  Variable = c("Age", "Income"),
  Mean = c(34.5, 65000),
  SD = c(8.3, 15000)
)

# Save as TSV
write_table(df, "results/descriptives.tsv")

# Save as markdown
write_table(df, "results/table1.md", format = "md")

# Save as RDS (preserves types)
write_table(df, "results/data.rds", format = "rds")
}

}
