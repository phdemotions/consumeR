% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical_tests.R
\name{odds_ratio_table}
\alias{odds_ratio_table}
\title{Odds Ratio with Confidence Interval}
\usage{
odds_ratio_table(data = NULL, x, y, conf_level = 0.95)
}
\arguments{
\item{data}{Data frame (optional)}

\item{x}{Character or vector (exposure/treatment variable)}

\item{y}{Character or vector (outcome variable)}

\item{conf_level}{Numeric. Confidence level (default 0.95)}
}
\value{
Tibble with odds ratio, CI, and interpretation
}
\description{
Calculates odds ratio with confidence interval for 2x2 contingency tables.
Common in case-control studies and binary outcome analysis.
}
\details{
**Odds Ratio (OR):**
- OR = 1: No association
- OR > 1: Positive association (exposure increases odds of outcome)
- OR < 1: Negative association (exposure decreases odds of outcome)

**Interpretation:**
- OR = 2: Exposure doubles the odds of outcome
- OR = 0.5: Exposure halves the odds of outcome
}
\examples{
\dontrun{
# Treatment effect on success
result <- odds_ratio_table(
  data = df,
  x = "treatment",  # Exposed vs unexposed
  y = "success"     # Outcome
)
}

}
