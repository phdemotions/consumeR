% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_formatting.R
\name{make_table_tsv}
\alias{make_table_tsv}
\title{Create TSV Table for Copy-Paste}
\usage{
make_table_tsv(df, digits = 2, include_rownames = FALSE, na_string = "")
}
\arguments{
\item{df}{Data frame to convert}

\item{digits}{Integer. Number of digits for rounding numeric columns (default 2)}

\item{include_rownames}{Logical. Include row names? (default FALSE)}

\item{na_string}{Character. How to represent NA values (default "")}
}
\value{
Character string containing TSV data (invisibly)
}
\description{
Converts a data frame to tab-separated values (TSV) format that can be
easily copied and pasted into spreadsheet applications like Excel or
Google Sheets.
}
\details{
The output is printed to the console where it can be copied.
Use Ctrl+C (or Cmd+C on Mac) to copy the output, then paste into Excel/Sheets.

Alternatively, write to clipboard on supported systems:
\code{writeClipboard(make_table_tsv(df))} on Windows
\code{clip <- pipe("pbcopy", "w"); write(make_table_tsv(df), clip); close(clip)} on Mac
}
\examples{
\dontrun{
# Create table
df <- data.frame(
  Variable = c("Age", "Income", "Satisfaction"),
  Mean = c(34.5, 65000, 7.2),
  SD = c(8.3, 15000, 1.4)
)

# Print as TSV
make_table_tsv(df)

# Copy the output and paste into Excel/Sheets
}

}
