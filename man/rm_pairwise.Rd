% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rm_anova.R
\name{rm_pairwise}
\alias{rm_pairwise}
\title{Pairwise Comparisons for Repeated Measures ANOVA}
\usage{
rm_pairwise(
  rm_anova_result,
  factor = NULL,
  p_adjust = "bonferroni",
  conf_level = 0.95
)
}
\arguments{
\item{rm_anova_result}{Result object from \code{run_rm_anova()}}

\item{factor}{Character; which factor to compare (if multiple within-subjects factors)}

\item{p_adjust}{Method for multiple comparison correction: "bonferroni" (default),
"holm", "hochberg", "hommel", "BH" (Benjamini-Hochberg), "BY", "fdr", "none"}

\item{conf_level}{Confidence level for CIs (default: 0.95)}
}
\value{
A tibble with pairwise comparisons including:
  - comparison: Which levels are being compared
  - mean_diff: Mean difference
  - se: Standard error
  - t_value: t-statistic
  - p_value: Adjusted p-value
  - p_value_raw: Unadjusted p-value
  - conf_low, conf_high: Confidence interval
  - cohens_d: Effect size (paired)
}
\description{
Performs pairwise comparisons following a significant repeated measures ANOVA.
Uses appropriate corrections for multiple comparisons.
}
\examples{
# After significant RM ANOVA
result <- run_rm_anova(df, outcome = "satisfaction", within = "time", subject = "id")
pairwise <- rm_pairwise(result, factor = "time")
print(pairwise)
}
