% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psychometrics.R
\name{run_cfa}
\alias{run_cfa}
\title{Run Confirmatory Factor Analysis (CFA)}
\usage{
run_cfa(model, data, estimator = "MLR", missing = "fiml", std.lv = TRUE, ...)
}
\arguments{
\item{model}{Character string specifying the CFA model in lavaan syntax}

\item{data}{Data frame containing observed variables}

\item{estimator}{Character. Estimator to use (default "MLR" = robust ML)}

\item{missing}{Character. Missing data handling (default "fiml" = full information ML)}

\item{std.lv}{Logical. Standardize latent variables? (default TRUE)}

\item{...}{Additional arguments passed to lavaan::cfa()}
}
\value{
lavaan fit object (class lavaan)
}
\description{
Wrapper around lavaan::cfa() for confirmatory factor analysis.
Provides sensible defaults for consumer research while allowing
full customization via pass-through arguments.
}
\details{
**Recommended defaults:**
- estimator = "MLR": Maximum likelihood with robust (Huber-White) SEs
- missing = "fiml": Full information maximum likelihood for missing data
- std.lv = TRUE: Standardize latent variables (makes loadings interpretable)

**Alternative estimators:**
- "ML": Standard maximum likelihood (faster, but not robust)
- "WLSMV": Weighted least squares (for categorical data)
- "ULS": Unweighted least squares (no distributional assumptions)

**lavaan model syntax:**
```
# Latent variable defined by items
satisfaction =~ q1 + q2 + q3
loyalty =~ q4 + q5 + q6

# Covariances
satisfaction ~~ loyalty
```
}
\examples{
\dontrun{
# Define CFA model
model <- "
  # Latent variables
  satisfaction =~ sat1 + sat2 + sat3
  loyalty =~ loy1 + loy2 + loy3

  # Covariance
  satisfaction ~~ loyalty
"

# Run CFA
fit <- run_cfa(model, data = customer_survey)

# Get fit indices
fit_indices <- tidy_cfa_fit(fit)
}

}
